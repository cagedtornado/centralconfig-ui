deployment:
  release:
    branch: master
    commands:
    - mkdir /tmp/js
    - mkdir /tmp/css
    - cp css/*.css /tmp/css
    - cp js/bundle.js /tmp/js
    - cp index.html /tmp/index.html
    - zip -r9 $CIRCLE_ARTIFACTS/centralconfig-ui.zip /tmp/
    - go get github.com/tcnksm/ghr