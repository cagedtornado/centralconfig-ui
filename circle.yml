deployment:
  release:
    branch: master
    commands:
    - mkdir -p /tmp/centralconfig-ui/js
    - mkdir -p /tmp/centralconfig-ui/css
    - cp css/*.css /tmp/centralconfig-ui/css
    - cp js/bundle.js /tmp/centralconfig-ui/js
    - cp index.html /tmp/centralconfig-ui/index.html
    - cd /tmp; zip -r9 $CIRCLE_ARTIFACTS/centralconfig-ui.zip centralconfig-ui
    - go get github.com/tcnksm/ghr